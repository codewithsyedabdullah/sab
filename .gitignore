__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv/
venv/
*.log
.DS_Store
Thumbs.db
.ruff_cache/
.pytest_cache/
.mypy_cache/
node_modules/

# Runtime data (auth hashes + stored API keys) - never commit
data/

# Build-time artifacts: embedded python runtime + packaging output
desktop/runtime/
desktop/out-pack/
desktop/out-build/

# Offline seed files (Whisper model ~139MB exceeds GitHub's 100MB/file limit).
# The model is baked into the installers at build time via prepare-runtime.ps1,
# so it lives only on disk / in release assets, not in the repo.
offline/

# Local launcher scripts (not for distribution)
run_sab.bat
run_sab.vbs
start.bat
start.ps1
launch.py

# Logo design sources (final assets live in static/)
logos/
